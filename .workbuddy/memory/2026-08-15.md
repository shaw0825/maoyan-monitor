# 2026-08-15

- 项目介绍：maoyan-monitor 是猫眼《奥德赛》IMAX 场次监控（monitor.py + maoyan.py + config.py），从 taopiaopiao-monitor 改造而来。
- 新增企业微信群机器人推送功能（与邮件通知并存）：
  - config.py 新增 `WECHAT_BOT_ENABLED` / `WECHAT_BOT_WEBHOOK` / `WECHAT_BOT_KEY` / `WECHAT_BOT_MSGTYPE`(text|markdown)
  - monitor.py 新增 `send_wechat_text()` / `wechat_dry_run()` / `_wechat_webhook_url()` 和统一通知入口 `notify(subject, body)`（多渠道隔离，单渠道失败不影响其它）
  - `--test-mail` 现在同时测试邮件+微信两个渠道
  - webhook API: https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=...，errcode!=0 抛 RuntimeError；text 上限 2000 字符、markdown 4096，超长自动截断
  - 已用本地 mock 服务器验证 payload 格式、key 拼接、截断、errcode 错误处理；`--once` 端到端跑通（当前 27 个 IMAX 场次）
  - 运行环境：venv 在 /Users/xiaojm/.workbuddy/binaries/python/envs/default（已装 requests 2.34.2）
